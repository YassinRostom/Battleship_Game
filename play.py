# Import everything from a module named 'classes'.

from classes import *

# Create an instance of the Game class, which is defined in the 'classes' module.
game = Game()

# Call the 'start' method of the 'game' instance.
game.start()
